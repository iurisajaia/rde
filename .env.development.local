# Vite dev: leave API/WS unset — /rde-api → proxy → http://127.0.0.1:20000/api (see vite.config.ts).
# Optional — call API directly (CORS enabled on API):
# VITE_API_BASE_URL=http://127.0.0.1:20000/api
# VITE_WS_URL=ws://127.0.0.1:20000/api/ws
