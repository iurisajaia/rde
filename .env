# Local browser development only (npm run dev:browser)
# These vars tell the Vite dev server (port 5173) where the Express server is (port 3000).
# Do NOT set VITE_API_BASE_URL for production builds — the app uses same-origin /rde-api (see vite.config.ts).
